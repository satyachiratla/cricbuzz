import "./Addscoreform.css";

const Addscoreform = (props) => {
  return (
    <form className="form">
      <div>
        <button>Add Runs</button>
        <input type="number"></input>
      </div>
    </form>
  );
};

export default Addscoreform;
