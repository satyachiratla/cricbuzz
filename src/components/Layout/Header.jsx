import { Fragment } from "react";
import '../Layout/Header.css';

const Header = (props) => {
  return (
    <Fragment>
      <header className="header">
        <h1>Cricbuzz</h1>
      </header>
    </Fragment>
  );
};

export default Header;
